'use strict'

describe('safe box', function () {

    // safeBox('123', 'my secret'); 
    // returns true - if password is correct, indicating secret is saved and safe
    // throw Error('wrong password') - if password is wrong

    it('should safe de secret in de the safeBox', function () {

        safeBox();
        // 0 1
        // 1 2
        // 2 3

        expect(result, array, true);

        var expected = { 0: 1, 1: 2, 2: 3 };

        expect(array, expected, true);
    });


    it('', function () {


    });





}